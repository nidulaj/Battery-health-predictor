services:
  backend:
    volumes:
      - ./backend:/app
    environment:
      FLASK_ENV: development

  frontend:
    env_file:
      - ./frontend/.env
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
